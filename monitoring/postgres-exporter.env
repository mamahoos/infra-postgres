# postgres_exporter connection settings (optional — not part of core compose)
# Enable via compose.monitoring.yaml when Prometheus is available.
#
# Example service env:
DATA_SOURCE_NAME=postgresql://postgres:change_me@postgres:5432/postgres?sslmode=disable
PG_EXPORTER_EXTEND_QUERY_PATH=/etc/postgres-exporter/queries.yaml
